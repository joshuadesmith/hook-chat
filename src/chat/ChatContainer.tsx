import React from "react";
import { Button } from "@material-ui/core";

function ChatContainer() {
    return (
        <div>
            <Button variant="contained">OMEGA CHAT</Button>
        </div>
    );
}

export default ChatContainer;